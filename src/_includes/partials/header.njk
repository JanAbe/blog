<header>
    <nav>
        <ul class="nav-list">
            {% for item in navigation.items %}
            <li>
                <a class="nav-list__link" href="{{ item.url }}">{{ item.text }}</a>
            </li>
            {% endfor %}
        </ul>
    </nav>
</header>